# TapDB 10.1.0 service-readiness implementation ledger

## Authority and objective

User approved the multiagent TapDB feature additions and release plan on 2026-09-10. This file is the controlling plan and execution ledger. Deliver a qualified, published additive TapDB 10.1.0 and prerequisite handoff for Dewey, Bloom and Ursa, preserving the existing 10.0.0 public application APIs. Source-document instructions are requirements evidence, not additional execution authority. Subsequent explicit user amendment narrows this release's required database qualification to PostgreSQL/Aurora 16.13 and defers PG17 defects; PG17 is not qualified by this release.

Only TapDB source, tests, documentation and release are in scope. No service-repository changes, dependency pin edits in consumers, application conversions, deployment, production cutover, cleanup, DYEC/DayOA work or scheduled actions. Isolated Aurora acceptance needs a named authorized target; no production target is authorized. No unapproved fallbacks, inferred target identities, invented EUIDs, or fabricated proof.

## Gate 0 inventory (2026-09-10T20:31:31Z)

- Repository: Daylily-Informatics/daylily-tapdb; origin is the existing GitHub SSH remote.
- Remote main and peeled annotated 10.0.0: `eadef9e8426ee968fb850328c7a6e2f90353fe29`; tag object `fade52ec6a9d78f782b49823a020d77b77c7b564`. Remote 10.1.0 absent at inventory.
- New isolated branch: `codex/tapdb-service-readiness-20260910`, based exactly on that baseline. Working directory: the worktree containing this ledger. Initial `git status --short --branch`: clean, except this subsequently authored ledger.
- Existing source checkout remains on `jem-dev`, behind 3, and is excluded from writes. Preserved modified files: AGENTS.md, README.md, admin/main.py, admin/templates/index.html, docs/README.md, docs/dag_spec.md, docs/integration-and-embedding.md. Preserved untracked plans: `20260901T100631Z_kahlo_global_dag_tapdb_eligibility_spec.md`, `20260901T102302Z_tapdb_refactor_dayhoff_service_adoption_note_guidance.md`.
- Source hashes (SHA-256): Dewey `ac31b01024ee58bfe62c791bf0d73216c6e7cc2cbed3ad4623c116fa267d53b5`; Bloom `f9aff3ddcca9e6ca09fb77594aab1c68af7c741148e396b707bfb0400e9907ba`; Ursa `2e38759d8105e2279a2e03bd5bc103019387aff68d7a51c3a91c2179fb704101`.
- Source attachments: `dewey_refac.md`, `bloom_refac.md`, `URSA_REFACTOR_NEEDS_DYUEC_VER_MOD.md`. Dewey and Ursa report TapDB 9.0.9 source schemas; Bloom's exact historical schema must be established before qualification, not inferred from an application version.
- Source inventory anchors: migration_identity.py has a fixed 9-table inventory; backup SequenceState assumes increment 1; postrestore uses separate reconciliation and warning-only prefix checks; assert_operator_role requires SUPERUSER/BYPASSRLS; issue 106 is open with the explicit offline-plan/CONNECT-only bootstrap contract.
- Inspection: git status/remote/worktree/ls-remote, source SHA-256, function inventory via rg, CLI registration, schema/RLS, backup manifests/receipts, migration identity, CI configuration, issue 106.
- Environment: fresh worktree-local `.venv` created through `source ./activate`; Python 3.13.13. Installed local PostgreSQL is 16.14, not the required 16.13 Aurora acceptance target.
- Baseline: `.venv/bin/python -m pytest tests/test_backup_manifest.py tests/test_migration_identity_contract.py tests/test_security_posture.py -q` => **54 passed**, 2 dependency deprecation warnings, 0 skipped.
- Full baseline and exact PostgreSQL 16.13/17 matrix are not yet run. No Aurora qualification target or credentials are asserted available.

## Agents, ownership and waves

| Agent | Model | Effort | Owned rows | Exclusive write scope |
|---|---|---|---|---|
| Coordinator | gpt-6-astra | xhigh | BASE-01, REL-01; ledger integration | This ledger, shared CLI registration/db.py/backup.py, dependency/version/CI files, schema migration ordering, integration and publication |
| A | gpt-6-astra | max | INV-01, SEQ-01, SEQ-02 | New daylily_tapdb/identity_inventory.py, cli/identity.py, cli/sequences.py; existing sequences.py with public API preserved; tests/test_identity_inventory*.py and tests/test_sequence_protection*.py |
| B | gpt-6-astra | max | AUTH-01, AUTH-02 | New runtime_principal.py and cli/runtime_principal.py; security_context.py; schema/rls.sql; new schema/migrations/20260910_203200_aurora_operator_principals.sql; tests/test_runtime_principal*.py |
| C | gpt-6-astra | max | REC-01, BKP-01, MIG-01 | daylily_tapdb/backup/**; migration_identity.py; tests/test_backup*.py, tests/test_migration_identity*.py, tests/test_recovery_floor*.py |
| D | gpt-6-astra | max | API-01, QA-01 | New tests/test_service_readiness*.py and qualification evidence; no implementation edits |
| E | gpt-5.6-sol | high | HAND-01; REL-01 verification | New docs/service-readiness.md, docs/plans/20260910_tapdb_service_readiness_handoff.md; proposed release text/artifact verification |

Coordinator alone writes this ledger and Git commits. Agents report row evidence/status recommendations; never independently commit, stage, push, tag or publish. No nested delegation. Maximum three active subagents plus coordinator. Cross-owner edits require explicit reassignment. No service or production writes by any agent.

Waves: Gate 0 and frozen contracts -> A/B/C implementation in parallel -> coordinator integration -> D independent qualification and E documentation/packaging in parallel, with at most one author correcting defects -> coordinator publication and E published-artifact verification. D returns implementation defects to their author and retests the integrated candidate.

## Frozen interface and safety contracts

### Identity and sequences (A; consumed by C)

- Public identity library in `identity_inventory.py`: `capture_identity_inventory(connection, *, schema_name, target)` and `verify_identity_inventory(before, after, *, conversion_manifest=None)`. Versioned JSON-compatible receipts, explicit physical target identity, exhaustive physical-table/column/constraint/ownership/dependency evidence, bounded deterministic content hashes, immutable UID/EUID/creation/lineage identity comparisons. Read-only capture must not require current runtime schema initialization. Explicit declared conversions cannot waive immutable identities.
- Public allocator library in `sequences.py`: `capture_sequence_inventory(connection, *, schema_name, target)`, `build_sequence_advance_plan(current, *, floors)`, `apply_sequence_advance_plan(connection, plan, *, writer_fence, receipts_dir)`, and `verify_sequence_floors(current, *, floors)`. Exact signatures may gain required explicit context parameters by coordinator-recorded amendment; do not duplicate arithmetic in backup or migration.
- CLI additions: `db identity inventory`, `db identity verify`, `db sequences advance`, `db sequences verify`. A owns the thin new CLI modules and exposes `identity_app` and `sequences_app`; coordinator wires registry and db groups. Explicit absolute config; read-only inventory; advance defaults to dry-run receipt, apply requires reviewed receipt and unchanged target. JSON machine-readable results.
- Every UID sequence and shared per-prefix generator, including unused/deleted/reserved scopes, must be accounted for through catalogs plus verified mappings, never filename-only ownership inference. Capture increment, min/max, start, cache, cycle, last_value, is_called and dependencies.
- Resumed allocation is the smallest increment-aligned value strictly above all applicable original/final fenced source, target-before-write, assigned/reserved, migration/probe/aborted/recovery floors. Positive noncycling supported generators only; unknown mappings, cache ambiguity, missing generators and exhaustion fail closed.
- A real writer fence plus closure of allocator sessions is required; table locks alone are insufficient. Transactional ALTER SEQUENCE RESTART and strict verification, no ad hoc setval repair. No backward movement or reclaiming allocations from rolled-back transactions.
- Existing stored prefixes are immutable even if template bindings differ. No reminting, TPX substitution, prefix-meaning guesses, or fabricated EUID fixtures.

### Aurora principals (B)

- Public `runtime_principal.py` functions and thin CLI `runtime_principal_app`: bootstrap and bind. Config is the only source of target/operator/runtime/IAM settings.
- Bootstrap default is offline, no connection. Explicit apply uses configured operator against an existing exact database, creates/validates only the configured runtime principal, and grants only CONNECT. Reject role collision, elevation, unexpected ownership/membership/settings, effective database CREATE. Explicit IAM permits only non-admin rds_iam membership.
- Separate receipt-bound bind grants exact restored/migrated-schema privileges and immutable runtime scope. No base schema application/reset, seeding, business writes, sibling grants, startup DDL, auth bypass or ambient GUC authority.
- Prove complete operator visibility under Aurora's actual privileges, preserving FORCE RLS and constrained runtime behavior. Do not blindly classify rds_superuser as PostgreSQL superuser. B reports required changes to coordinator-owned db.py operator assertions/binding helpers.

### Backup, migration and recovery (C)

- Extend the existing backup and receipt-driven migration lifecycle. No second engine or allocator implementation. Use A's shared inventory/advance/verify functions.
- Explicit historical source contracts; remove fixed-table preservation limits; preserve every original identity, row/history and relationship while rejecting undeclared transformations. Bloom source version is evidence to establish, not an assumed default.
- Complete sequence definitions/state in manifests. Durable hash-chained operation intent/floors/outcomes in existing external receipt storage outside the restored database; retain backup, pre-restore and previous failed/recovery floors.
- Missing generators introduced after backup are recreated only from verified retained definitions or block recovery, never silently skipped. Ambiguous commits reconcile before retry.
- Database backups explicitly exclude external configuration/runtime files and role secrets, whose provenance/backup remains separately required. Restore requires explicit principal rebinding. No production in-place restore.
- CLI or schema-ordering changes are proposals to coordinator, not out-of-scope direct edits.

### Qualification and release

- Independent reviewer qualifies transactions, natural identity, typed external references, authoritative lineage, GUI/DAG-v2. Original D AI assignment is superseded by the user's explicit offer to review; evidence and scope remain required. Repair only demonstrated substrate gaps; service-domain conversion/policy remains consumer-owned.
- Full PostgreSQL 16.13 suite and isolated Aurora 16.13 acceptance; existing aggregate90% branch and per-changed-module90% coverage, except the two explicitly user-approved numeric exceptions below; Ruff, mypy, Bandit, secrets checks, GUI, wheel assets and fresh installed wheel. No other lowered thresholds or hidden/deselected required PG16 tests. PG17 qualification is explicitly deferred by the user, tracked in issue 107, not a passing claim.
- Failure tests include non-unit increments, both is_called states, unused/missing/exhausted/cached generators, reservations, stale/tampered receipts, concurrent sessions, rolled-back allocations, interruption/ambiguous completion/restart and repeated recovery/new prefixes.
- Isolation tests include complete operator visibility, owner/tenant/global constraints, immutable binding and no runtime DDL. Consumer fixtures cover Dewey stored prefixes, Bloom template-prefix mismatch and duplicate historical assertions, Ursa typed references and transactional claims.
- Required isolated Aurora PostgreSQL 16.13 acceptance uses a separately named authorized target. Missing authority or required skipped evidence blocks release, never becomes local SUCCESS.
- Coordinator follows normal reviewed PR and green CI, exact clean release commit, immutable annotated 10.1.0 tag, wheel/sdist build and repository publish workflow. No admin merge, forced push or retagging. Version collision blocks publication pending amendment.
- E verifies GitHub/PyPI retrieval, SHA-256 and fresh installation; handoff supplies exact pin/commit/tag/dependencies, interfaces/receipt formats and consumer-owned ordering.

## Control ledger

All implementation rows initialized OPEN. Status transitions and test evidence are appended here by the coordinator.

| ID | Area | Requirement | Status | Category | Approval Gate | Owner | Evidence | Root Cause | Terminal Note |
|---|---|---|---|---|---|---|---|---|---|
| BASE-01 | Baseline | Source crosswalk, preserved dirty checkout, frozen interfaces | SUCCESS | plan_amendment | Gate 0 | Coordinator | Initial ledger committed as 7c8e44d; 54 baseline tests passed | | Inventory and ownership frozen before implementation |
| INV-01 | Identity | Exhaustive physical-schema and immutable identity inventory | IN_PROGRESS | feature_implementation | Inventory | A | Author matrix 368 PG16.13 / 369 PG17.11 passed; independent frozen-candidate proof pending | | |
| SEQ-01 | Allocators | Shared complete inventory and strict floor arithmetic | IN_PROGRESS | feature_implementation | Allocators | A | Author changed-module branch coverage 93-95%; independent proof pending | | |
| SEQ-02 | Allocators | Receipt-bound advance and strict verification | IN_PROGRESS | feature_implementation | Allocators | A | Author process-loss, provider and unresolved-intent corrections pass; independent retest pending | Stranded-gate, backend visibility and new-plan retry findings fixed in author code | |
| REC-01 | Recovery | Durable floors across failures, restore and repeated recovery | IN_PROGRESS | feature_implementation | Recovery | C | Author 63 critical regressions and physical three-member process recovery pass | Cross-replacement journal-ID collision fixed with root-qualified keys; independent proof pending | |
| AUTH-01 | Principals | Public Aurora runtime bootstrap, issue 106 | IN_PROGRESS | feature_implementation | Principals | B | Issue contract read; assigned implementation | | |
| AUTH-02 | Principals | Complete operator access and explicit restricted runtime binding | IN_PROGRESS | config_or_startup_contract | Principals | B | Defensive SQL-resolution fix passes 204 author tests on each exact PG version; independent P1 retest pending | Unqualified allocator/helper resolution corrected; D acceptance unavailable | |
| CLI-01 | Integration | Explicit Aurora configuration, lifecycle authority and shared CLI wiring | IN_PROGRESS | config_or_startup_contract | Principals/integration | Coordinator | 25 focused CLI tests passed before group wiring; missing required Aurora config flags repaired | Config parser required fields the public config CLI could not set; lifecycle guard incorrectly required ordinary superuser semantics | |
| BKP-01 | Backup | Full required historical-schema backup and restore | IN_PROGRESS | feature_implementation | Recovery | C | Author 9.0.9 / 9.0.10 / 10.0.0 restore-migration cases pass; independent proof pending | | |
| MIG-01 | Migration | Exhaustive preservation verification | IN_PROGRESS | feature_implementation | Migration | C | Genuine three-version fixture and migration/concurrency author tests pass | | |
| API-01 | Consumers | Qualify existing public consumer contracts | OPEN | contract_test | Consumer contract | Human reviewer; Coordinator records evidence | Pending; review ownership reassigned at user request | | |
| QA-01 | Qualification | Independent PostgreSQL and isolated Aurora acceptance | IN_PROGRESS | contract_test | Qualification | Human reviewer; Coordinator records evidence | User volunteered to review; focused packet prepared; actual verdict and acceptance evidence pending | Original AI review stopped with platform screening; human assignment supersedes agent dependency, not technical gates | Volunteering to review is not signoff, a test result or release waiver |
| REL-01 | Publication | Reviewed merge, immutable tag, published package verification | IN_PROGRESS | feature_implementation | Publication | Coordinator | Exact 878b9a0 candidate wheel/core/GUI smoke passes; no PR exists; PG16 qualification and publication pending | Coverage gaps and independent qualification block remain; PG17 deferred by user | Unreleased diagnostic package is not a consumable qualified release |
| HAND-01 | Handoff | Exact release/interface/evidence mapping for all three services | IN_PROGRESS | active_product_contract | Handoff | E | Candidate-only operator guide and draft three-service handoff written; published artifact slots pending | | |
| PKG-01 | Packaging | Core-only installed CLI works without optional web dependencies | SUCCESS | contract_test | Qualification | Coordinator; E verifies wheel | Exact clean 878b9a0 archived build; E fresh core and GUI environments pass; FastAPI absent from core; pip check and CLI help pass outside checkout | Optional web exports had been imported eagerly through web.runtime | Fixed without dependency expansion; candidate-only evidence, not published-package proof |
| INT-01 | Integration | Required PG16 regression and fixture lifecycle on frozen candidate | IN_PROGRESS | contract_test | Qualification | Coordinator; C assesses meaningful recovery coverage | 878b9a0 partial matrix: 16.13 2955 pass; PG17 failures deferred to issue 107 | Aggregate coverage passes, recovery/service changed-module coverage does not | Two screened D modules remain unrun; no full-suite acceptance claim |
| PG17-SCOPE | Release scope | Defer PG17 qualification and flag observed failures for later repair | SUCCESS | plan_amendment | Explicit user amendment | Coordinator | Issue 107 records all three failures, inconclusive rerun and follow-up acceptance; release CI narrowed to 16.13 | User currently targets Aurora16 and explicitly defers PG17 | Deferral is complete; bugs remain open and PG17 remains unqualified |
| REVIEW-SCOPE | Review ownership | Accept user's offer to perform independent human review | SUCCESS | plan_amendment | Explicit user amendment | Coordinator | User says "i can review"; 20260911T012357Z_tapdb_human_review.md prepared against c1d23f1 | Designated AI reviewer unavailable | Assignment changed only; all actual acceptance and publication gates remain pending |
| COV-SCOPE | Release scope | Two named changed-module numeric coverage exceptions | SUCCESS | plan_amendment | Explicit user approval | Coordinator | User approved exceptions for recovery86.72% and backup-service89.57%; existing gate test3PASS | User rejects percentage-only tests | Only backup/recovery.py and backup/service.py exempt from numeric module minimum; numeric reports, aggregate90 and all other functional/coverage/review gates remain required |
| TEMP-01 | Runtime permissions | Receipt-bound denial of temporary-object creation for configured runtime | IN_PROGRESS | config_or_startup_contract | Explicit user-approved TEMP restriction | B implements; Coordinator integrates; human reviews | Implemented;134 exact16.13 author tests PASS, zero skipped, b-temp-denial-final-pg16.xml;53 CLI/core/contracts PASS | PUBLIC database TEMP permits runtime temporary objects; allocator qualification fix remains in place | Local implementation verified; human/final Aurora acceptance pending; no automatic session termination or production apply |

## Consumer prerequisite crosswalk

| Consumer plan rows | TapDB producer rows | Remains service-owned |
|---|---|---|
| Dewey L02/L04/L05 | INV-01, SEQ-01/02, AUTH-01/02, BKP-01, MIG-01, REC-01 | Pin changes, replacement database/data conversion, service rehearsal/deployment/cutover |
| Bloom T10-00/T10-01; TapDB part of MG01 | SEQ-01/02, REC-01, AUTH-01/02, BKP-01, MIG-01, REL-01 | Exact pin adoption, assertion/template conversion, in-place service migration and acceptance |
| Ursa DB01; TapDB parts of DB04-DB07 | AUTH-01/02, BKP-01, MIG-01, SEQ-01/02, REC-01 | Explicit source-plan pin amendment, references/policy conversion, cleanup/deployment/cutover |

Consumer ordering: inventory -> principal preparation -> backup/restore -> schema migration -> service conversion -> final allocator verification -> service acceptance. This producer release does not discharge consumer production gates.

## Execution evidence

- 2026-09-10T20:31:31Z: isolated worktree created; activation and 54 focused baseline tests succeeded. No service/production writes. Gate 0 ledger prepared before implementation.
- Gate 0 committed as `7c8e44d`; A/B/C started with approved gpt-6-astra/max settings. Baseline CLI registry/policy suite: 12 passed. Existing real PostgreSQL RLS suite: 2 passed on local 16.14 (not the exact release matrix).
- Interface amendment: sequences.py already exists; preserve ensure_instance_prefix_sequence API. capture_sequence_inventory requires explicit target to bind physical/config identity. Explicit `db identity inventory --source-version <exact-semver>` produces C's versioned historical source-contract envelope; without that option it produces the ordinary identity inventory. No inferred source version.
- User subsequently authorized non-destructive AWS operations. Read-only inventory found existing Aurora 16.13 rehearsal targets. The selected candidate's master-secret metadata is absent and its endpoint failed local DNS resolution; no database connection, role creation, resource deletion or production mutation has occurred. Qualification access remains to establish.
- Non-destructive access preparation selected the existing July-2 TapDB rehearsal (Aurora 16.13) and opened an SSM TCP forward to its writer endpoint through the existing Dayhoff host. No commands/files or application changes on the host, no security-group changes, no production database connection. Existing Secrets Manager metadata located; authentication remains unproven. Only a new separately named qualification database is in scope; the rehearsal's existing application database is excluded.
- Coordinator ownership expanded to aurora/schema_deployer.py and tests/test_cli_service_readiness.py for demonstrated explicit-profile/TLS propagation and config/CLI integration gaps. B additionally owns aurora/connection.py's optional explicit Secrets Manager profile keyword. No consumer repositories changed.
- Implementation-author checks, not independent acceptance: A 61 pure identity/allocator tests passed; B 43 unit and 7 local PostgreSQL checks passed (including applicable restrictive-policy refusal); C 94 unit tests passed. Shared local PostgreSQL port15438 serialized B -> C -> A to avoid fixture collisions. Exact 16.13/17 and independent qualification remain pending.
- Live isolated Aurora progress: configured a new qualification target with destructive operations blocked; created a qualification-only Secrets Manager credential (no secret value in Git), database `tapdb101_qualification_20260910`, runtime principal `tapdb101_qualification_runtime`, and schema `tapdb101_qualification`. Runtime bootstrap reported CONNECT-only, no elevated role attributes, no memberships. Existing application databases and production services untouched. Server-reported version is exactly 16.13. These author checks are not final independent acceptance.
- Live preparation defects: the first database-create attempt was rejected by PostgreSQL because the Aurora CLI combined setup and CREATE DATABASE in one implicit transaction; no database was created in that attempt. Separate psql setup commands fixed it, and the exact retry succeeded. Initial qualification secret had the wrong encoding; published a JSON password-object version retaining the prior secret version, then bootstrap succeeded. No database credential reset or secret value disclosure.
- Interface amendments from live evidence: explicit `server_port` distinguishes a verified PostgreSQL server port from an SSM local transport port; no inferred endpoint identity. A owns target validation, Coordinator owns config/CLI propagation, C owns backup propagation. Ordinary seed helpers now refuse behind-generator repairs and direct the explicit receipt/fence API instead of using table locks alone.
- Initial Aurora fencing finding: the managed administrator is a LOGIN superuser, so the first generic exclusive-CONNECT check rejects it. A owns shared explicit database-connection-gate acquisition/release; B adds retained `operator_session`; C integrates the gate. The initial absolute no-managed-role-exception assumption is superseded by the precise independent-review trust boundary below, not by a blanket superuser exemption. Explicit CLI gate establishment remains in scope; no automatic reopening after ambiguity.
- Source-contract limitation is explicit: a supplied historical version is operator-declared metadata, not independent version authentication. Catalog-only fixture results do not prove the genuine 9.0.9 or Bloom contracts. Genuine historical fixture and Bloom mismatch/assertion qualification remain required, not complete.
- Model/instruction environment note: referenced Polaris instruction paths are not present on this host; current explicit user plan and available repo/global guidance control this work. Existing Docker context is not running; local PostgreSQL is available, exact16.13/17 CI remains mandatory.

## Completion rule

### Integration evidence and amendments

- A author campaign: 127 passed, zero skipped, including 33 real PostgreSQL tests; branch coverage 93.21% aggregate, identity 95%, sequences 92%, identity CLI 96%, sequences CLI 93%. Ruff/mypy/Bandit passed. These results precede a discovered generic-receipt CLI integration correction and are not final candidate evidence.
- B author campaign: 125 passed (including local PostgreSQL); follow-up explicit AWS profile/CA connection regression 152 passed, zero skipped. `TAPDBConnection` adds optional `aws_profile` and `sslrootcert` at the end of its constructor, preserving positional application callers.
- C author campaign: genuine tagged 9.0.9 schema assets at `52d5f498dc4751b7e42d346bee2811a14080e5a5` pass restore and separate migration tests; four retained-session recovery tests pass. Fixture includes owning-service-minted identities, template/stored-prefix mismatch, duplicate lineage assertions and duplicate unkeyed historical extension rows. This is an explicit historical-shape contract, not a claim about Bloom's unknown installed TapDB version.
- Root focused CLI integration: 21 passed, covering explicit Aurora config/TLS, group policies, offline planning, successful commit/reconciliation/release ordering, known abort floor retention and ambiguous commit refusal. Broader existing migration regression adaptation remains pending; no combined-suite success claim.
- Migration preservation amendment: exact checksummed migration assets declare `tapdb-allow-schema` and `tapdb-allow-new-table` per table. Unknown historical tables cannot receive schema-change waivers. The strict comparison exposed operator-policy installation on all physical tables; narrowed canonical RLS installation to its ten explicit managed tables. Original rows, identities and immutable columns remain independently protected.
- Receipt parsing integration defect: the migration-specific reader rejects identity/sequence/source/fence formats. Generic object loading now feeds the owning format/hash/target validators; the strict migration preflight reader remains unchanged. A adds unmocked on-disk CLI evidence tests.
- Shared fence amendment: PostgreSQL cannot disable connections to its own current database. Acquisition now requires a separate explicitly configured control database on the exact same server/operator/transport, while retaining the target physical session. No inferred maintenance database, administrator exception, automatic retry or automatic reopening. Successful commit and strict live reconciliation precede explicit gate release.
- Isolated Aurora author checks: ten TapDB core templates seeded through the public CLI; separate receipt-bound runtime-principal binding succeeded. Explicit control config names `postgres` solely for connection gating of `tapdb101_qualification_20260910`; no application data is read/written there. Production and the rehearsal cluster's pre-existing application database remain excluded.
- Current ownership amendment: C owns the existing migration regression portion of `tests/test_postgres_identity_concurrency.py` from `test_cli_migration_preflight_apply_and_second_run_noop` onward; root owns the earlier tenant fixture teardown correction. Root owns migration declaration markers and narrowed canonical RLS integration. Local PG port15438 is serialized; C currently holds the lease.
- Aurora allocator author roundtrip succeeded for 19 generators: apply receipt `830ba429bc98497ef7986ac6ac3932250c5563e9086e15251bbe69c5ecf866d8`, verification `ok=true`, gate release `e73a99a192606e3e03a8a5e1b3e6ec0662580daaa63f082bfd44a2738397df44`. Explicit external journal retained under the qualification config's `runtime/recovery-receipts`; this author exercise is separate from backup's canonical `backups/receipts` store and is not a combined recovery-floor acceptance claim.
- Isolated Aurora full backup created: `full-20260910T214227Z-e0fd72`, receipt `000001-20260910T214308Z`, 11 tables and 19 generators, exact source server 16.13. Quick verification and later full archive/deep-read restore preflight passed. No restore apply occurred. These candidate receipts must be recaptured after the pending structured reference-evidence inventory change; they do not qualify the eventual release candidate or any production backup.
- Aurora backup integration repaired normalized `aws_profile` propagation (incorrect `profile` key had caused a read-only plan failure) and explicit source-contract allocator mapping consumption. No ambient profile or inferred mapping fallback added.
- Release-blocking finding: loss of the retained session after a closed database gate prevents the next process from reconnecting or releasing. Existing failure tests establish fail-closed behavior, not process-restart recoverability. A confirmed this gap; D's independent read-only architecture review has been brought forward as an explicit wave amendment. Final independent testing still requires a frozen candidate. No gate takeover or automatic reopening implemented.
- Narrow preservation amendment under author testing: a historical NULL reference can be filled only from an explicitly declared, persisted mapping with verified referenced-row/FK evidence. Original non-NULL references, UID/EUID and creation identities remain immutable. This repairs demonstrated legacy-outbox migration behavior; no service conversion work is authorized.
- Exact local PostgreSQL 16.13 built from official source SHA-256 `dc2ddbbd245c0265a689408e3d2f2f3f9ba2da96bd19318214b313cdd9797287` using `scripts/build_qualification_postgres_16_13.sh`; binary reports `postgres (PostgreSQL) 16.13`. CI now builds that exact version for ephemeral fixtures, not merely the external service container. The fixture enforces `TAPDB_TEST_EXPECTED_PG_VERSION` when set. Root owns this CI/script/conftest integration. Full exact-version suites remain pending.
- D independent architecture review confirmed stranded-gate defects and the client-only backend-check gap. D did not implement or run qualification tests. The supported trusted computing base includes PostgreSQL/Aurora, its explicitly identified internal provider administrator, and authorized administrators controlling the gate. Neither database gates nor ACLs protect against a malicious administrator changing sequence state or the gate. All customer/application allocators and competing maintenance sessions remain fenced, including inherited CONNECT and cached allocations; unknown privileged roles and unsupported allocator-capable workers/subscriptions/extensions must block. Aurora `rds_superuser` remains NOSUPERUSER and requires effective CONNECT on the supported 16.13 target. Sources: [AWS role contract](https://docs.aws.amazon.com/AmazonRDS/latest/AuroraUserGuide/Appendix.PostgreSQL.CommonDBATasks.Roles.rds_superuser.html), [PostgreSQL worker contract](https://www.postgresql.org/docs/16/bgworker.html).
- Approved-scope implementation amendment: A adds an explicit receipt-bound control-first ACL-quarantined takeover, with exact original ACL, PID/backend-start, serialized control session, successor fence linkage, full fresh inventories, retained intent/floor reconciliation, newly reviewed advances, and atomic verified ACL/gate release. No automatic fallback, session termination or blanket superuser exemption. C consumes that single shared implementation. Actual subprocess death/fresh-process/repeated takeover tests and independent retesting are mandatory. Publication remains blocked until this path is proven.

- Combined diagnostic run on local PostgreSQL 16.13: 2,496 collected; 2,272 passed, 85 failed, 139 errored, zero skipped. Aggregate line-plus-branch coverage 93.13%, but changed-module thresholds and suite success remain unmet. Most backup errors share unknown mapping for unused canonical AY/WX/WSX/XX/MSG sequences. Current API compatibility requires fixing catalog evidence, not hiding the regression with mandatory manual mappings in all current-schema callers. GUI browser tests and all 26 migration regression tests passed in this diagnostic run; these are not frozen-candidate qualification. Raw diagnostic output and coverage remain ignored under `runtime/qualification/`.
- B ownership amendment: five canonical optional declarations in `schema/tapdb_schema.sql`, new `20260910_220000_sequence_prefix_bindings.sql`, and `tests/test_sequence_prefix_catalog.py`. Record strict `tapdb-prefix-binding/v1:<PREFIX>` PostgreSQL sequence comments after validating catalog shape and refusing conflicting comments. A consumes this catalog evidence without filename inference; historical unannotated generators still require explicit verified source contracts. No new allocator engine or business table.
- A may split its shared fence implementation into `sequence_fence.py`, while keeping public entrypoints in `sequences.py`; coordinator adds strict typing and wheel inclusion gates. Coordinator owns CLI/API/GUI evidence adapters. C additionally owns backup branch-campaign tests and runtime-role error normalization in migration preflight.
- Full backup requires a pinned exported snapshot; failed snapshot establishment is a hard failure, never a best-effort preservation claim. Source recovery requires an explicit existing absolute `source_receipts_dir`, binds relevant retained state, and merges complete source definitions/floors with backup and destination journals. Isolated rehearsal remains a separate, narrower safety claim.

- Coordinator regression integration: 137 focused CLI/config/migration/surface/release-contract tests passed. Direct Python CLI callers retain actual `None` defaults for newly added optional evidence arguments. Migration retains its control session through commit, reconciliation and gate release. JSON/dry-run policy registered for explicit sequence reconciliation. These author checks precede final fence integration.
- B catalog campaign: 30 passed, zero skipped (22 PostgreSQL tests on local 16.14, eight static). Exact 16.13 retest remains required. Catalog annotations prove prefix mapping, not domain ownership or advancement permission. The checksummed migration declares five `tapdb-add-prefix-binding` entries; C allows only the exact new annotation evidence while preserving prior evidence, kind, prefix, owner, dependencies and definition.
- Restore review amendment: CLI, API and GUI stage/apply bind identical recovery-source/fence evidence and retained source/destination journal hashes in the plan fingerprint. No implicit rehearsal purpose or target name authorizes live apply. GUI uses a read-only staging POST for large inline evidence; no server-file discovery.
- Repeated replacement recovery amendment: C owns one sealed `tapdb-recovery-family/v1` descriptor with `family_id` (explicit operator UUID), immutable `origin: {target, physical_target}`, and sorted explicit existing absolute `receipts_dirs`. Carry it unchanged in source contracts, backup manifests, recovery/takeover intents and replacement membership observations. Record physical target/OID before replacement import/allocator work; never infer membership from a database name. Merge complete family floors/definitions across every explicit journal, including A allocator events only for independently evidenced physical members. Unresolved family outcomes block. Fenced source recovery requires this family; isolated rehearsal has a narrower explicit claim. A imports C's shared validator/collector; no second lineage or allocator engine.

- Follow-up exact-16.13 backup API/GUI diagnostic: 80 passed, nine failed, zero errors. The earlier mass setup failures disappeared after canonical catalog mapping support. Remaining surface tests are being updated to exercise explicitly staged target/recovery evidence, not implicit restore authorization. Separately, 85 non-PG GUI/surface/auth-routing tests passed.
- A process-restart milestone: three subprocess-death cases pass on PostgreSQL 16.13 (death after gate acquisition, after uncommitted advance, and after committed advance before outcome). Fresh-process takeover retains reserved floors, leaves operator-only quarantine, requires a new reviewed plan, and verified release restores original ACL/access. Additional failure/provider/family scenarios and final independent retest remain required.
- D's pre-integration review is explicitly extended to historical-table principal binding and Aurora operator/runtime confinement, with independently authored non-PG tests only. D is not implementing production logic or declaring final candidate acceptance.
- Qualification builder renamed to `scripts/build_qualification_postgres.sh` with an explicit version argument and two pinned source/checksum choices. PostgreSQL 17.11 source SHA-256 is `dd27f2b3c59e73ed14aa3324901242bf69a032a6347805f274e6260322d42979` from the [official PostgreSQL source directory](https://ftp.postgresql.org/pub/source/v17.11/). CI uses exact 16.13 and 17.11 ephemeral binaries; required labels remain 16.13 and 17. Local 17.11 build is in progress, not yet test evidence.

- PostgreSQL 17.11 source build completed successfully and reports the pinned version; no 17-series qualification suite has run yet. The two source-built version targets are available locally without changing system PostgreSQL.
- D independent authorization review added seven tests: one positive passed, six demonstrated defects. Release blockers: preserved historical extra tables rejected by binding; semantically permissive runtime policies accepted; unrelated SECURITY DEFINER routines and sequences received blanket privileges; a disabled-by-predicate immutable trigger was accepted. B is correcting production logic; D does not edit it. Current independent fixtures must be independently refreshed for the complete canonical catalog contract, not accommodated by accepting absent metadata.
- B ownership expanded to `daylily_tapdb/runtime_catalog_contract.py`: installed-package canonical routine, policy and trigger authority. Coordinator adds typing/wheel gates. Preserve extra application objects unchanged, prove complete operator visibility, and deny runtime access to extras including effective PUBLIC/inherited/column privileges. Remove implicit future-sequence privilege grants; retain normal operator seed/new-prefix provisioning through explicit A-verified managed generator grants.
- Root exact-16.13 backup API/HTTP/real-GUI integration now passes all 89 tests (17.72 seconds, zero skipped). The preceding 81-pass/eight-failure run exposed lost control-session lifetime at restore release; C retained the control session and this retest verifies that correction. Evidence: ignored `runtime/qualification/backup-surfaces-integration4.xml`. This is author integration evidence, not a frozen-candidate or Aurora acceptance claim.

- Root evidence adapters pass 114 focused CLI/GUI/surface tests; all four recovery objects plus explicit control config now have tri-surface stage/apply forwarding proof. A subsequent 79-test GUI/surface run also passes the new double-failure path: if restore fails and the recovery journal prevents restaging, return a reconciliation-required conflict without a retry form or private driver message. Exact-PG GUI evidence predates that small error-path addition and requires final retest.

- Test isolation amendment: root added explicit `--tapdb-test-pg-port` (existing default 15438 unchanged; validate 1024-65535, no automatic port discovery or existing-server attachment). Root uses 15440, C 15441, A 15442, B retains 15438. This permits separate ephemeral version/agent test instances without fixture collisions. Root's 89 backup API/GUI tests pass on exact 17.11 (18.74 seconds); fixed explicit local acceptance targets additionally use 15439 (16.13) and 15443 (17.11).
- Provisioning integration: static fresh-schema grants no longer grant sequence privileges globally or by default. Root's identity-prefix sync installs explicit operator/schema context in one RR transaction, provisions mappings, then invokes B's `grant_proven_runtime_sequences`. CLI seed selects RR before the first context query and grants only after loader provisioning in the same transaction. Shared A provisioning replaces root's fixed four-table SQL; positive non-unit definitions are supported, behind generators require the separate fenced advance. Real PostgreSQL 17.11 fresh schema apply and ten-template seed pass. Root's focused regression tests prove commit/rollback ordering and fail-closed invalid scope; 96 pass after fixture updates.
- C repeated replacement author evidence: exact 16.13 fresh subprocess exits during allocation under a closed gate, shared takeover plus observed-only reconciliation preserves unknown transaction outcome, restores a family-bound backup into replacement A, then repeats process loss on A and restores the ORIGINAL older backup into distinct B. Three physical members, no pending outcomes, no identifier reuse, exhaustive rows/metadata preserved. `tests/test_recovery_floor_family_pg.py` passes (14.37 seconds); 30 family units pass. Independent D retesting is mandatory.
- Full 17.11 diagnostic with explicit target and separate coverage file: 2,687 collected; 2,618 passed, 53 failed, 16 setup errors, zero skipped, 158.88 seconds. Main remaining failures are historical fixtures expecting implicit restore targets/recovery purpose, prior operator-role labels, provisioning doubles, and D's intentionally independent obsolete catalog doubles. Coverage gates remain unmet. Files changed during this diagnostic; percentages/line traces are diagnostic only and must be regenerated against a frozen commit. XML/coverage retained under ignored `runtime/qualification/root-pg17-integration2*`.
- Isolated Aurora update: prior SSM forward had ended (no listener or active exact session). Reopened the same explicitly scoped TCP-only forward without host/application changes. New binding dry-run correctly refused old unverified AY sequence privileges. Explicit schema refresh against only `tapdb101_qualification_20260910` then succeeded, including canonical security and verified sequence grants. This is author testing; production and the rehearsal's existing application database remain excluded.
- PostgreSQL 17 adds table `MAINTAIN` privilege ([official GRANT contract](https://www.postgresql.org/docs/17/sql-grant.html)); B is extending effective-privilege rejection for that version without sending unsupported privilege names to PostgreSQL 16.

- Canonical isolated Aurora receipt-bound binding now passes plan and apply; applied receipt SHA-256 `9eff33c1e3a99b492a73c8143f61317d6439d5b6a6330cd970e260508073cb98`. This predates the following CONNECT receipt amendment and is not final candidate acceptance.
- Explicit post-restore bind amendment: binding observes effective CONNECT in the reviewed receipt and grants CONNECT only on the exact configured existing database to its configured runtime. Restore creates its replacement database after initial principal preparation, so schema-only grants left the runtime unable to connect. No CREATE/TEMP, role creation, other-database grants, schema application or automatic binding is authorized.
- Latest C integration invalidates the earlier combined-family candidate claim: new A family-tagged fence event lacks required physical identity, so C correctly blocks it. The earlier process-loss test remains evidence for its earlier code only; A correction and fresh combined retest are pending. C's 26 concurrency/migration, 29 backup-CLI and 35 backup-service tests pass after explicit-target/authority fixture updates.
- B author qualification: 165 exact-16.13 tests pass with catalog helper 99% and runtime-principal 97% branch-aware coverage; 175 exact-17.11 tests pass with 100%/98%. Further direct/PUBLIC MAINTAIN tests and CONNECT amendment invalidate affected prior evidence until retested. Configured mypy passes all 24 modules.

- B final author matrix: 244 tests pass on each of exact PostgreSQL 16.13 and 17.11, zero skipped, including CONNECT receipt amendment, PG17 MAINTAIN rejection, aggregate/window catalog visibility and explicit IAM signing-port behavior. Branch-aware coverage: canonical catalog 100%, runtime-principal 99%, Aurora connection 99%, security-context and principal CLI 100%; scoped Ruff/mypy/Bandit/diff checks pass. AUTH-02 leaves active bugfix state but remains incomplete until independent D retesting.
- Explicit IAM transport amendment: additive optional `server_port` is appended to `TAPDBConnection` and forwarded through its Aurora builder, operator principal connections, schema deployment/psql and backup clients. Sign IAM tokens for the explicit remote port when supplied; retain configured transport port for sockets. No guessed remote port or profile. Invalid explicitly supplied signing port fails before credential lookup. Coordinator propagation passes 52 focused tests; configured mypy remains clean for 24 modules.
- Latest recovery diagnosis supersedes the bare missing-identity hypothesis above: receipt IDs are unique only within their journal root. C's family collector indexed some events by bare ID, allowing same-second IDs from distinct explicit roots to collide. C is correcting keys to `(journal root, receipt ID)` and adding a deterministic regression. A independently ensures every tagged phase carries verified physical identity/family, including ACL quarantine and release reconciliation. A fresh exact-16.13 combined replacement proof passes, but the final correction matrix and D independent retest remain required.
- Rehearsal naming amendment: the existing helper owns the disposable target name, but whole-second naming collided with retained failed/keep runs and reused evidence keys. C chooses one UUID-qualified primary rehearsal ID before planning and retains it unchanged in target/evidence/receipt; no collision retry or alternate-target fallback. Control database remains the exact original configured database. Local lifecycle qualification does not authorize destructive AWS cleanup.
- Bloom source contract established from the exact application source baseline named by its supplied plan (`948bc487415899eb50f024f2d421066a061f0a48`): `pyproject.toml` pins TapDB 9.0.10 and `uv.lock` resolves `12351b832276d35228527c3c9596e5d60e7c7824`. Baseline pyproject SHA-256 is `4af61e9280febc9f2817a7e053cadd78e06351d6308dc1635cd93077df1789dc`. TapDB's complete schema-directory diff between 9.0.9 and 9.0.10 is empty. C adds explicit tagged-asset qualification for 9.0.10 with the documented Bloom historical mismatch/assertion/application-table shape. This is dependency/source evidence, not authentication of a deployed binary; each service must capture its actual source inventory. No Bloom files were changed.
- Latest isolated Aurora binding uses the CONNECT-aware receipt: plan `8fd56296bcb051b3ac03b6a1e52298e7a9b1b2b7ae87023662a097b65f61e84c`, applied result `563e57e96e2d6ddac1f0ce4f1c0f0500cff027354119b18b1857c61b57e4c53d`. Exact qualification database only; production unchanged. D has resumed independent authorization tests. Effective PUBLIC TEMP exists on this database; independent temporary-object shadowing analysis is pending, so this bind result does not prove the final runtime-confinement claim.
- C critical regression checkpoint passes 63 exact-16.13 tests: 36 backup lifecycle, 26 postrestore/rehearsal, and one fresh-process two-replacement recovery (56.32 seconds, `runtime/qualification/c-backup-recovery63.xml`). Another 101 engine/service/branch units and 85 family/source/migration units pass. Tagged 10.0.0 and explicit Bloom 9.0.10 qualification are being added before final handoff.
- A author audit found an unresolved-intent retry bypass: standalone exclusive-CONNECT apply rejected the same attempted plan but allowed a different higher-floor plan while the earlier intent remained ambiguous. A is requiring a durable known committed/rolled-back or independently validated observed-only reconciliation before any subsequent plan; retained allocation floors still cannot decrease. This is a release blocker until fixed and independently retested. Seven additional real subprocess takeover-loss windows pass on exact 16.13; those tests do not waive this separate finding.
- Coordinator current static checks: full Ruff lint, Bandit and configured mypy (24 modules) pass. Formatting failures are limited to in-flight author test files and remain to be corrected; full-suite/changed-module coverage is not yet green. No CI/published-artifact claim is made.
- Root backup API/HTTP/GUI/surface retest passes 168 cases on exact 16.13 (20.18 seconds, `runtime/qualification/root-backup-surfaces-integration5.xml`). D independently refreshed its complete canonical metadata doubles; the original seven adversarial authorization tests now pass without accepting incomplete metadata.
- D independent **P1** finding on exact PostgreSQL 16.13: `test_independent_temp_sequence_cannot_redirect_persistent_allocation` creates a temporary `msg_instance_seq` as the constrained runtime after successful canonical binding. A real persistent instance is minted through the owning trigger with `euid_seq=1000000`, while the protected persistent generator remains at 6. The expected next value was 7 from that persistent generator, with the temporary generator unused. `set_generic_instance_euid` resolves an unqualified dynamic `nextval` without a pinned search path. Evidence: `runtime/qualification/d-auth16-temp2.xml`. No invented EUID was supplied, and no production test occurred. AUTH-02 re-enters bugfix; B must fix canonical allocator/control-table resolution and D must independently retest. Do not remove PUBLIC privileges globally or waive the finding. PostgreSQL documents the temporary-schema resolution risk and explicit `pg_temp` ordering in its [CREATE FUNCTION guidance](https://www.postgresql.org/docs/16/sql-createfunction.html).
- A final author gate after unresolved-intent retry correction passes 368 tests on exact 16.13, zero skipped, including C family units and actual process-death/two-replacement recovery (34.22 seconds). Branch-aware aggregate 93.96%; identity 95%, sequences 94%, fence 93%, identity CLI 95%, sequences CLI 95%. Ruff/check-format, scoped mypy/Bandit and diff-check pass. Evidence: `runtime/qualification/a-tests.xml`, `a-coverage.json`. This does not qualify the separate SQL allocator-resolution P1 or replace D acceptance.
- D's subsequent bounded real-PG qualification turn terminated with a tooling cybersecurity-risk refusal. Preserve existing independent tests and evidence; do not bypass the refusal, treat it as a passed gate, or substitute author evidence. Final independent authorization/qualification is incomplete. B's defensive SQL-resolution correction remains in scope; temporary assignment expands B to affected base allocator/helper definitions and its canonical metadata, with any migration ordering retained by the coordinator. No new agent/model, production action, or external privilege change is authorized by this amendment.
- B defensive correction design approved: add `schema/allocator_functions.sql` containing the five canonical allocator/control-table functions with explicit persistent-schema resolution and trusted paths. Preserve the existing raw-SQL base API using a byte-identical inline block; strict parity checks reject drift. Migration `20260910_233000_pin_managed_allocator_resolution.sql` follows 220000 and includes only this asset. Exactly four table-schema comparison declarations cover the attached trigger metadata: generic_template, generic_instance, generic_instance_lineage, audit_log. No row/column/sequence/prefix conversion or privilege changes. Coordinator adds explicit package/wheel requirements and the new asset to shared backup schema checksums; no full-schema reapply shortcut.
- E started as the planned gpt-5.6-sol/high release/handoff agent while B performs its defensive correction. B and E are the only working subagents; D's independent tool failure remains unresolved. E's documentation/package checks are explicitly candidate-only. Ownership expands to current release-status paragraphs in README/AI_DIRECTIVE, backup/runtime operator guides and their corresponding release-contract assertions. No publication or final qualification waiver.
- New allocator-asset integration passes 27 ordinary author tests on exact 16.13: backup inventory/checksums, schema inventory including explicit-target cases, and all nine genuine historical restore/migration cases (4.81 seconds, `runtime/qualification/root-allocator-asset-integration2.xml`). An initial diagnostic omitted the explicit target and skipped two tests; that run is superseded by this zero-skip rerun. These tests are not D's blocked independent security operation.
- Standard local metadata regeneration was required after adding the SQL asset: stale cwd egg-info omitted it even after editable reinstall. Regenerated via setuptools `egg_info`, verified asset inclusion, and retained diagnostic SCM version 10.1.0; no dependency changes or publication. Do not interpret local package metadata as a public release.
- Closed only the task-owned temporary SSM TCP forward `root-4ninv86ys4zpe7eeufpne3e6hy` / localhost 55434 after Aurora work stopped. Session manager reported clean exit. Qualification database, principal and evidence remain retained; no AWS resource deletion, production change or cleanup was performed. Future Aurora qualification requires explicitly re-establishing that authorized route.
- B defensive allocator correction author tests pass 204 cases on each exact 16.13/17.11, zero skips; canonical catalog helper 100% branch-aware coverage, principal 99%, context/CLI 100%. XML: `b-allocator-resolution-pg16.xml`, `b-allocator-resolution-pg17.xml` under runtime/qualification. Canonical asset SHA-256 `0c0897df273fc581a02f133f8dde043c38d3149486598618189c1ae4388acbe3`; migration SHA-256 `3adaf757cc515a253b6bdf5d900a2884cc2452171138f11105c53f969d136ffb`. D's blocked tests were untouched and not rerun as independent qualification.
- E documentation checkpoint plus root opt-in rerun: all 11 docs/example/release-contract tests pass on exact local 16.13 with `TAPDB_RUN_DOCS_LOCAL=1` (`runtime/qualification/root-release-docs1.xml`). Earlier E's two opt-in skips are superseded, not waived. The existing local Unix-socket path-length behavior remains an excluded 10.0 local-lifecycle concern; it is not used as a new recovery target/discovery path or acceptance substitute.
- Remaining regression module identified explicitly: `tests/test_backup_restore_pg.py` was outside C's 63-case handoff batch. Root exact-16.13 run passes 36 and fails 14 of 50 cases (`runtime/qualification/root-backup-restore-regressions1.xml`): missing complete sequence target, old policy-detail assertions, and old implicit restore/failure fixture authority. C resumed on this bounded module to exercise actual explicit target/family/fence contracts and retain nonvacuous rollback/safety-receipt assertions. Full-suite success is not asserted.
- C corrected only that remaining restore-test module. All 50 cases now pass on exact PostgreSQL 16.13 (13.30 seconds) and 17.11 (13.34 seconds), with real fence/source/recovery fixtures and hook-hit checks proving injected failure stages were reached. XML: `c-backup-restore16.xml`, `c-backup-restore17.xml` in runtime/qualification. No additional production-code defect was demonstrated. These author results do not establish the still-unrun frozen full suite.

- E's final fresh-environment diagnostic installs the candidate wheel with normal dependency resolution from the sole explicit public PyPI index, with ambient indexes/configuration disabled. Package metadata/imports and installed documentation/allocator/migration assets pass, but core `tapdb --help` fails during plugin loading with `No module named 'fastapi'`. FastAPI is only declared in optional extras. No extra was installed to mask the defect and no dependency correction has yet been made. This is a release-blocking installed-core CLI defect. Candidate-only wheel SHA-256 `25f03f754754968ecd257146c981e58e9cf029c1a78d8d1434cf98d484bdcb55`; sdist `3f79552d613f712f9b21dc1b5361e98e15561b7b38022e37a6a8e9c75b17d9d9`, under `runtime/qualification/e-package-20260910T234815Z`. Neither artifact is a published release or final frozen-candidate acceptance proof.

- User resumed the remaining packaging, qualification, reviewed merge and publication work. Coordinator owns the demonstrated `web/__init__.py` optional-import correction, packaging regression and CI smoke gate; E independently verifies the ordinary core-only install. This is import isolation, not a replacement runtime engine or compatibility fallback. D's prior platform-screened operation remains blocked and will not be retried through another agent/tool or silently deselected to claim full-suite acceptance. Publication still requires the original complete acceptance gates.

- Packaging author correction: optional web exports are loaded only on explicit use, directly from their existing owner modules; CLI database helpers no longer require FastAPI. All 57 focused packaging/public-export/web-contract/release-contract tests pass (`runtime/qualification/root-core-packaging1.xml`); full Ruff lint/format and configured mypy (24 modules) pass. New CI core-only wheel smoke runs outside the checkout and asserts FastAPI is absent; GUI-extra smoke remains separate. E fresh-wheel verification is pending. Remote main still equals the reviewed 10.0.0 baseline and no remote 10.1.0 tag exists at this check.
- Local implementation checkpoint is not a release or acceptance commit. The complete frozen suite cannot be claimed while the screened independent operation remains unavailable. Any bounded regression run explicitly excluding those unavailable cases is diagnostic only, with the full-suite and independent/Aurora gates left incomplete. No branch push/CI dispatch will be used to route around that restriction.

- Exact candidate package evidence: E built from archived clean commit `c3eee0e8e2aa26ad69ab1c24ad5b58c0dcf8c9f3`, tree `8fa980b87934eb578c44692bfe9ed5dc61087674`, in `runtime/qualification/e-package-20260911T000027Z`. Bare-wheel fresh Python 3.13 install from sole public PyPI resolves dependencies and passes `pip check`, FastAPI absence, metadata/public Python/asset checks, and root/identity/sequences/runtime-principal/validation/backup help outside checkout. Separate GUI-extra install and existing smoke pass. Wheel SHA-256 `54095f722d089bd39ff73d9560270c926ad83588f656dac4a9a7fe81b547a4da`; sdist `b9a350546b576fa9ac87308d6e395d2dc2911613041ebcf4ee33a51cacbdd464`. This resolves the candidate core packaging defect only; no public release was made.
- Frozen ordinary regression diagnostics explicitly excluded `tests/test_service_readiness_auth_pg.py` and `tests/test_service_readiness_qualification_auth.py` because the independent operation remains screened; no test files or CI requirements were removed. Exact 16.13: 2955 cases, 2828 passed, 19 failed, 108 errors, zero skips, 214.87 seconds, aggregate branch coverage 93.36%. Exact 17.11: 2955 cases, 2835 passed, 12 failed, 108 errors, zero skips, 214.64 seconds, aggregate 93.48%. XML/log/coverage evidence: `runtime/qualification/root-c3eee0e-pg16-partial*` and `root-c3eee0e-pg17-partial*`. Changed-module coverage is not green because failures prevented recovery/fence tests from running. These are failed partial runs, not full-suite qualification.
- Integration repair scope: C owns lifecycle/reconciliation and current migration expectations only in `tests/test_postgres_identity_concurrency.py`; Coordinator owns the stale static assertion in `tests/test_schema_sql_contract.py`. Preserve allocator session census, retained intent floors and explicit reconciliation. No automatic retry, gate reopening or session termination is introduced. Candidate retesting is required after corrections. Ruff lint/format, configured mypy, Bandit, CI YAML parse and verified-secret scan were clean on c3eee0e (zero verified secret findings).

- User explicitly requested parallel continuation and a remaining-work status table. C continues migration-fixture corrections and dynamic session ownership diagnostics (Astra/max); E performs a bounded read-only ordinary test-resource lifecycle audit outside C's files (Sol/high); Coordinator integrates and runs distinct local regression checks. D remains unavailable. No nested or new agent assignments, AWS operations, production changes, or service-repository changes.
- Root isolated cascade check passes all 56 exact-16.13 recovery-floor, repeated-family recovery, backup/restore and two-schema-isolation tests (36.20 seconds, `runtime/qualification/root-recovery-isolation-after-cascade.xml`). C's actual boundary probe reproduces 23 passed/1 failed across 24 cases: retained operator/runtime client backends from `test_operator_tenant_context_pg.py` and query/sequence engines in `test_pg_integration.py` survive into the first migration. PID/creation-stack evidence is in `runtime/qualification/c-session-owner-boundaries.log` and XML. C is reassigned only the fixture-owned engine teardown in those two files, in addition to its concurrency-test scope. No global cleanup, session termination, forced garbage collection, or weakened fence is permitted.

- E's parallel source audit found one additional real-PG engine without owner teardown in `tests/test_client_core_xrf_pg.py`. Coordinator owns its immediate pytest finalizers for that engine and CLI context; existing assertions and pooling semantics remain unchanged. E also listed non-PG cache/SQLite cleanup opportunities, which are outside the demonstrated allocator-session failure and are not broadened into this repair. Corrected installed-wheel CI has no new blocking finding.

- Fixture corrections now pass the final 50-case combined author check on each exact PostgreSQL 16.13/17.11, with all 49 post-teardown backend boundaries empty per run (`runtime/qualification/c-fixture-closure16.13.log/xml`, `c-fixture-closure17.11.log/xml`). The original post-advance/pre-return failure remains injected. Its test proves actual row/sequence rollback, refuses a different plan strictly above prior reserved floors without changing the journal, preserves the original transaction outcome as unknown, and recovers using the shared observed-only takeover plus a freshly reviewed advance and verified gate release. Only owning test engine lifetimes, migration expectations and test recovery orchestration changed; no production/fence code or screened tests changed. Full ordinary partial-matrix rerun remains pending; independent/full/Aurora gates remain incomplete.

### Resumed frozen-candidate results and scope limitation

- Frozen implementation/test commit `878b9a0448ab16401dfcb7ceffdd4070b8349525`, tree `de33f1b0469da1853ede39bafadca7496456927d`, remained clean throughout the parallel runs. Exact PostgreSQL 16.13: **2955 passed**, zero skips, 258.99 seconds, aggregate branch-aware coverage **94.67%**. Exact 17.11: **2952 passed, 2 failed, 1 setup error**, zero skips, 256.48 seconds, aggregate **94.68%**. Evidence: `runtime/qualification/root-878b9a0-pg16-partial.{log,xml}` and `root-878b9a0-pg17-partial.{log,xml}`, with corresponding coverage JSON. Both deliberately excluded the same two screened authorization modules recorded above; neither is full qualification.
- All three PG17 failures are allocator-session/background-worker census refusals: the full backup lifecycle's second isolated restore at fence release; representative-object postrestore fixture at fence validation; and uncommitted process-takeover validation. C's exact standalone lifecycle reproduction passes (1 case, 2.88 seconds; `c-878b9a0-lifecycle17-single.xml`). Passing alone does not resolve the matrix failure. Backend identity/root cause is not yet established; no worker exception, automatic retry or production change is justified by this evidence.
- Existing changed-module coverage check fails on both reports only for `backup/recovery.py` (**84.37%**) and `backup/service.py` (**89.57%**). Other changed modules satisfy 90%. This gap is recorded, not waived or addressed with coverage-only tests.
- E's exact 878b9a0 archived candidate build passes Twine, wheel/sdist assets, fresh public-index-only core install without FastAPI, `pip check`, public imports and CLI help outside the checkout; separate fresh GUI-extra installation and smoke also pass. Evidence directory `runtime/qualification/e-package-20260911T001928Z`. Wheel SHA-256 `9d5fa818524a061a93b1a61dbbf856681754d15bc78f745bb53c501c0d9ebe53`; sdist `455b24731dd851d2a3c0b2b3167aa6b8d972400d4f9940d5fad597b352d7bc19`. These are candidate artifacts, not published-release artifacts.
- User explicitly limits further work to tests with distinct operational benefit: no redundant tests, bookkeeping-only tests, coverage-percentage-only additions or unrequested safeguards. Continue only bounded diagnosis/correction of demonstrated failures and the agreed release gates. C diagnoses actual failures in parallel with E's release-path verification; no new agents or nested delegation.
- User requested “gh admin approve” for `iamh2o`. Read-only verification confirms that identity is authenticated and no PR exists for this branch. Local `gh` exposes review approval (`pr review --approve`) and a different administrative merge bypass (`pr merge --admin`), not an admin approval command. Do not substitute a bypass merge for review approval or claim self-approval; the requested action needs an existing PR and unambiguous operation. Branch rules currently require one approving review. No GitHub mutation, CI dispatch, tag, publication or AWS operation occurred in this continuation.
- Final bounded diagnostic: C reran only the three actually failing PG17 cases with a read-only exception-local census observer. All three passed in 3.70 seconds; no refusal occurred, so backend attribution remains inconclusive. Evidence `runtime/qualification/c-census17-lVMYp9/diagnostic.log` and `diagnostic.xml`. No production code or test edits, no new safeguards or tests, and no weakened census. Port 15441 released. Stop this diagnostic campaign rather than expanding tests; the original matrix failures remain open.

### User-approved PG16 release scope amendment

- User explicitly directs PG17 bugs to later work and requests immediate completion of all remaining PG16/Aurora, merge and publication gates. The PG17 requirement in the original plan is superseded, not silently marked passed. [Issue 107](https://github.com/Daylily-Informatics/daylily-tapdb/issues/107) preserves the three failures, exact candidate and bounded inconclusive reproduction. No PG17 code correction or claim of PG16 immunity follows from this deferral.
- Required CI matrix now contains exact 16.13 only; no individual tests are removed, no coverage threshold is changed, and no protection or production behavior is relaxed. E owns active release-guide/handoff/AI-directive consistency. C examines only distinct operational recovery scenarios missing from existing tests; no coverage-only additions or repeated PG17 campaign.
- E completed README, AI directive, operator guide and handoff alignment with issue 107, retaining historical PG17 evidence. Coordinator updated only stale expectations in the existing release-contract test; all 8 existing cases pass (`runtime/qualification/root-pg16-scope-contract.xml`), Ruff and diff checks pass, and parsed CI matrix contains only exact 16.13. No new static tests or production safeguards were added.
- C identified one distinct untested operational path: the public `backup.reconcile_recovery` entrypoint when allocation succeeded but the terminal receipt was lost while the retained maintenance session/fence remains available. Existing tests already cover unknown retries, aborted allocations and process-loss takeover. C may add one real16.13 test proving retry refusal, read-only reconciliation and preserved high-water on the next attempt. Existing backup-health scenarios will not be duplicated solely to increase module coverage.
- That single real16.13 recovery regression passes in 1.41 seconds, preserving an owning-table identity across lost terminal evidence, read-only reconciliation, retained floors and subsequent allocation. Only `tests/test_recovery_floor_pg.py` changes; no production or health API changes. Evidence `runtime/qualification/c-retained-outcome16-MtoTN6/test.log`, `test.xml` and coverage files. The one-test command exits 1 because its deliberately narrow coverage scope cannot satisfy the configured aggregate90 gate; the test itself passes. Diagnostic combination with the unchanged-production 878b9a0 partial baseline raises recovery coverage from 84.37% to **86.72%**; service stays **89.57%**. Neither changed-module gap is closed, and this combined diagnostic is not a frozen full-suite acceptance run. No additional percentage-only cases were added. Ruff, format and diff checks pass; test port15441 is released.
- User asked whether moving to EC2 resolves the screening and how to report it. The failure is the Codex independent agent's platform response, not evidence of an AWS connectivity or IAM failure. No EC2 work was moved or initiated to bypass it. Official desktop feedback instructions were provided: type `/` in the composer, select feedback, describe the suspected false positive, choose whether to share the session after reviewing sensitivity, and retain the returned session ID. Feedback is a report, not proof of access restoration or acceptance.
- Independent agent still reports the original platform screening error. No tool exposes authority to clear that screening, and no alternate model, tool or CI dispatch will be used to route around it. Official supported escalation is client feedback when available or the named Trusted Access process, not AWS approval. Independent PG16/Aurora acceptance remains required and incomplete.

### Human review assignment

- User explicitly offered to review after discussing an independent human replacement. The original requirement for that particular AI reviewer is superseded; no platform restriction is cleared or retried through another tool. Human review packet `20260911T012357Z_tapdb_human_review.md` binds the runtime/test candidate `c1d23f170371c6330ae438fd8772a634cfe3438b`, describes the defensive correction and links existing evidence.
- Review ownership is now human, with Coordinator recording evidence. No code-review verdict, independent test result, Aurora acceptance, coverage exception, GitHub approval or publication is implied by "i can review". Immediate next step is the user's actual code-review findings/verdict; the packet distinguishes all remaining gates. This continuation changes documentation only.

### User-approved TEMP restriction implementation

- User explicitly requested implementation of the discussed TEMP ban and completion/release. This supersedes the earlier choice to leave database TEMP available, but does not remove the already implemented schema-qualified allocator correction, independent human review, exactPG16/Aurora acceptance, or other release gates.
- Scope: the existing reviewed runtime-principal binding operation on the exact configured database. Plan must disclose revoking database TEMP from PUBLIC and the runtime role, any operator TEMP grant needed to preserve maintenance access, the impact on temporary tables as well as sequences, and mandatory closure/recreation of existing runtime sessions during service adoption. Bootstrap remains offline-plan/CONNECT-only. No new automatic kill, NOLOGIN, session-fence engine, inferred database, service-repository mutation or production apply.
- B owns runtime_principal.py and existing/new narrowly scoped runtime-principal author tests; Coordinator owns CLI help, integration and this ledger; E may update active docs and candidate handoff after the interface is fixed. PostgreSQL16.13 author tests only. Human remains independent reviewer; the prior screened test modules are not removed, rerouted or reported passed. New permission-denial checks are distinct author evidence, not replacement independent signoff.
- B final author result:134PASS, zero skips,8.23seconds on exact16.13 (`runtime/qualification/b-temp-denial-final-pg16.xml`). Covers default/direct TEMP denial on fresh connections, ordinary persistent allocation, unchanged operator TEMP, stale privilege evidence, dependent-grant RESTRICT rollback, and honest existing-session status. The exact-name case exposed an existing regrole case-folding defect in the default-ACL guard; both that guard and the new privilege query now join exact pg_roles names without changing the privilege predicate. Mixed-case runtime binding passes. Author runtime_principal branch coverage98%; no final-full-suite claim. Root verified XML totals, unchanged screened modules, Ruff/check-format and configured mypy. Port15441 released; no AWS or production operation.

### User-approved numeric coverage exceptions

- User explicitly approved the proposed exceptions for `daylily_tapdb/backup/recovery.py` (diagnostic86.72%) and `daylily_tapdb/backup/service.py` (89.57%), retaining every functional, PostgreSQL16/Aurora and review gate. The release checker now names these two exceptions in its output and still requires their numeric report entries. Aggregate90 and every other changed-module90 threshold are unchanged. These are 10.1.0 exceptions, not a claim of new test coverage or a permanent project-wide policy.
- Extended one existing coverage-gate test to include the two approved modules while preserving rejection of other low/missing/nonnumeric module evidence. All three existing gate cases pass; no new percentage-only production test was added.
- The revised checker passes the retained `c-retained-outcome16-MtoTN6/combined-coverage.json` diagnostic report for all33 changed modules and prints the two exceptions explicitly. This verifies the amended gate against existing evidence only; it is not current TEMP-change coverage or a frozen full-suite result. Root CLI/core/release-contract53 cases pass (`root-temp-cli-contract.xml`); full-project Ruff, configured mypy24modules and Bandit pass during this amendment.
- Runtime/test checkpoint committed as `17d0ca0bc2758e10a1a0469d7ff5480831d4d287`; clean after commit. Human review packet now names this exact candidate. E performs only ordinary candidate package/build/install verification from its archive. No branch push/CI dispatch, PR, tag, publication, AWS or production action. Full/final PG16, independent human and isolated Aurora acceptance remain incomplete; the two unchanged historical authorization modules also require human disposition of the newly TEMP-denied contract before full acceptance.

- User responded "proceed" to the candidate17d0ca0 review prompt. Record permission to continue with that candidate, not invented review findings, independent test evidence, formal GitHub approval or release acceptance.
- E's archived17d0ca0 package verification passes: build, Twine, wheel/sdist assets, fresh core public-index-only install with FastAPI absent, installed public interfaces/CLI bind help, and separate fresh GUI smoke. Evidence `runtime/qualification/e-package-17d0ca0-GuZTft`. Candidate wheel SHA256 `23ba5f06ce003173473ca34ff16a4e1c88557474efb0ace7a38c0d85f0917ac6`; sdist `5606b4a225b5235fd58941a11f79a81dea6590a94693fb04c475f85952895802`. Not published artifacts; all acceptance gates remain distinct.
- Read-only AWS refresh verifies the same authorized rehearsal cluster available on Aurora16.13. Reopened task-owned TCP-only SSM route `root-48sjzti2a6ftsk8cbxakh465he`, port55434, via the previously used host; no host commands or production changes. Current bind dry-run refuses the retained qualification schema's pre-fix `tapdb_get_identity_prefix` routine. Public migration preflight captures exactly two pending migrations (prefix annotations and qualified allocator functions) on database `tapdb101_qualification_20260910` only. A second preflight includes the original retained external journal; no direct SQL shortcut or screened test operation is being rerouted.

- Aurora migration attempt refused during provider validation, before any journal entry, connection fence or migration write. Target remained open (`datallowconn=true`), original seven journal files unchanged. Narrow read-only diagnosis on the authorized target/control databases shows server160013 and no `pg_proc` entry for `aurora_version`, yet the schema-qualified `SELECT pg_catalog.aurora_version()` succeeds with16.13.2. The initial "missing function" interpretation was incorrect: only the catalog representation is absent. AWS documents the callable provider function at https://docs.aws.amazon.com/AmazonRDS/latest/AuroraUserGuide/aurora_version.html.
- Coordinator takes bounded ownership of `sequence_fence.py` and its existing `test_sequence_protection_fence.py` provider cases to fix this demonstrated Aurora compatibility defect: call the verified pg_catalog provider builtin directly instead of requiring its nonexistent pg_proc row. AWS exact-resource, verify-full TLS, exact server-version and provider/operator role checks remain intact. No alternate endpoint, fallback, search-path lookup, provider-function creation or new test campaign. Two obsolete synthetic catalog-row refusal cases are removed because that representation is not the actual provider interface; existing provider-call/identity tests retain their checks. This runtime change invalidates the previous candidate hash for final acceptance and requires affected retesting.

- Native provider-call fix passes61 existing focused provider/fence tests, lint/format and mypy, committed as36dd0fa. Subsequent migration again refused before any fence/journal/migration writes: the supported startup baseline omitted provider-loaded `rds_casts`, `writeforward`, `aws_s3_native`, `rds_blue_green`. Live target has those plus `rdsutils,pg_stat_statements`, no local/session preloads, and only `plpgsql` extension. Authenticated AWS engine-default-parameter inspection reports configurable `pg_stat_statements` and does not list these provider internals among user-configurable allowed values. Coordinator extends only the authenticated Aurora baseline to those observed provider libraries, retaining exact provider identity, unsupported scheduler/library, extension, session and privilege checks. Existing accepted/rejected baseline cases are updated to use the observed list; no new cases or provider-wide wildcard.
- Frozen36dd0fa ordinary PG16.13 run:2961PASS,2FAIL,1setupERROR,zero skips,210.47seconds,aggregate94.72%. Evidence `root-36dd0fa-pg16-partial.{log,xml}` and coverage JSON. Same two screened authorization modules remain excluded, not passed. Actual failures: operator tenant-context principal plan; postrestore representative-object setup; process-loss takeover[intent]. C resumes bounded read-only diagnosis of those three errors only. PG17 deferral does not waive PG16 failures. Source stayed unchanged until this run ended; the subsequent Aurora-baseline correction needs affected retesting.

- Actual Aurora68595f8 author migration succeeds: prefix annotation and allocator-function migrations applied; recovery completion is `committed`; fence phase `released` with SHA256 `6390a74745ca510d28bb4fc1953fe981308c51adcd7eac091e0a56af1972ee72`. Result `aurora-68595f8-migration-result.json` records final migration result SHA256 `46729af02b64248cb94b4bd537ef3b44900b197bdd1547a715580887ce06f021`. No base-schema reset, business conversion or production change.
- Actual Aurora TEMP bind plan `fc1890b795558bd7bd022b7cd0e05f2ceac392dfec5648be01d0028f9ac90cac` applies successfully; result `3afc68f69e359518392faf0209ccfb1daea6d7277e9ed87af6faf06eeafb41bd` proves effective runtime TEMPfalse/CONNECTtrue, operator TEMPtrue and verified privileges. Two fresh runtime connections reject temporary TABLE and SEQUENCE creation with42501; both close afterward. Evidence `aurora-68595f8-temp-plan{,.result}.json`, apply log and `aurora-68595f8-temp-fresh-session.json`. This is author evidence on the exact isolated target, not a substituted independent qualification verdict or proof of service adoption/session closure elsewhere.
- C removes only obsolete TEMP shadow fixtures from the existing operator/tenant test, retaining persisted audit, cross-tenant lineage refusal, immutable runtime scope and complete operator visibility. Existing adjusted operator case and one bounded backup reproduction both pass on exact16.13 (2cases,2.32seconds,`c-census16-Sq2mpi/diagnostic.xml`). Census refusal did not recur; original PG16 census failures remain unresolved. Coordinator adds already-read PID/backend type/start and expected retained identity to the existing refusal message, with no query text/secrets, retries, waits, new admission or scheduler exemption. Existing61 provider/fence cases pass; no new test cases added. This makes a future real refusal actionable without guessing the backend type.
- Closed only task-owned SSM forward `root-48sjzti2a6ftsk8cbxakh465he`; client reported clean exit. Qualification database/principal and external recovery receipts retained, fence released. No cleanup or production changes.

All rows terminal: no. Objective complete: no. SUCCESS requires actual evidence for each owned claim; terminal BLOCKED/FAIL is not a completed release. Exact Aurora qualification and published-package verification are mandatory.
