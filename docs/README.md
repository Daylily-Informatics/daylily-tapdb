# daylily-tapdb Docs

## Current Reference Docs

- [../README.md](../README.md): repo overview and CLI surface
- [tapdb_gui_inclusion.md](tapdb_gui_inclusion.md): current embedding and auth-mode guidance

The packaged `daylily_tapdb/core_config` directory is the canonical TapDB core
template pack. Historical repo-root
`config/` template copies are no longer authoritative.

## Historical / Planning Docs

- `lsmc_tapdb_execplan.md`
- `lsmc_tapdb_breaking_changes.md`

Those files are retained for migration context. Treat the package code and the repo `README.md` as authoritative for current behavior.
