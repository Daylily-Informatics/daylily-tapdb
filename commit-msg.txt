feat: add domain_code + issuer_app_code columns to schema and ORM (Step 1)

- Add domain_code TEXT NOT NULL DEFAULT '' and issuer_app_code TEXT NOT NULL DEFAULT '' to generic_template, generic_instance, generic_instance_lineage, audit_log, outbox_event
- Add domain_code and issuer_app_code to tapdb_core ORM base class with server_default=FetchedValue()
- Add tenant_id, domain_code, issuer_app_code to outbox_event ORM model
- Update unique_template_code constraint to include (domain_code, issuer_app_code)
- Record meridian-euid 0.3.0pre dependency in pyproject.toml
- 623 tests passing
