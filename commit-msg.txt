feat: update connection/session context for domain_code + issuer_app_code (Step 2)

- TAPDBConnection accepts domain_code and issuer_app_code params
- Session vars: session.current_domain_code, session.current_app_code
- Rename sandbox_prefix to domain_code throughout euid.py (backward compat aliases kept)
- Env var MERIDIAN_DOMAIN_CODE replaces MERIDIAN_SANDBOX_PREFIX
- Domain codes expanded from 1 char to 1-4 chars
- format_euid() accepts domain_code= param
- validate_euid() accepts allowed_domain_codes= and environment='domain'
- 623 tests passing
